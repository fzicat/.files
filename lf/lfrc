set hidden
set scrolloff 8

map v $$EDITOR "$f"
map s $$SHELL
map W winstarta

map n down
map e up
map m updir
map i open

cmd winstarta ${{
    cmd.exe /C start "" "$f"
}}

cmd winstart ${{
    echo $f
}}
